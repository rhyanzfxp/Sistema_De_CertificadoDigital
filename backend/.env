# Variáveis de Ambiente para o Backend (Render)

# URL de conexão com o MongoDB Atlas
# O valor atual foi extraído do app.js e deve ser substituído por uma variável de ambiente segura no Render.
# Exemplo: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority&appName=<appname>
MONGODB_URI='mongodb+srv://soulzuhn:VtdaZTsbpduPEqlz@cluster0.izbfhx4.mongodb.net/sistema_certificados?retryWrites=true&w=majority&appName=Cluster0'

# URL base do Frontend (Vercel)
# Esta URL será usada para o QR Code e para a URL de verificação.
# Exemplo: https://seu-frontend.vercel.app
FRONTEND_URL=

# URL base do Backend (Render)
# Esta URL será usada para as chamadas de API do Frontend.
# Exemplo: https://seu-backend.onrender.com
BACKEND_URL=

# Porta em que o servidor irá rodar. O Render usará a variável PORT automaticamente.
PORT=3000
